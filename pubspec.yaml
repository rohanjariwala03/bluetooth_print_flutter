name: bluetooth_print_test
description: A new Flutter project.

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.1.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.6

  intl: ^0.18.1
  
  esc_pos_utils_plus: ^2.0.2
  bluetooth_print: ^4.3.0
  image: ^4.1.3
  bluetooth_thermal_printer: ^0.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true
